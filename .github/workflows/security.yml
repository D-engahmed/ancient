name: Security

on:
  pull_request:
  push:
    branches:
      - main
      - "production-hardening/**"
      - "production-ready/**"
  schedule:
    - cron: "17 3 * * 1"

permissions:
  contents: read
  security-events: write
  actions: read


jobs:
