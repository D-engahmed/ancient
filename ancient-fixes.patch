--- a/packages/agent/src/runtime/ExecutionEngine.ts
+++ b/packages/agent/src/runtime/ExecutionEngine.ts
@@ -1,5 +1,6 @@
 import { Team } from "../team/Team";
 import { Task } from "../tasks/Task";
+import { logger } from "../utils/Logger";
+import { CostTracker } from "../backends/CostTracker";
 
 export class ExecutionEngine {
   private activeExecutions: Map<string, Execution>;
+  private costTracker: CostTracker;
+  private rateLimiter: RateLimiter;
+  private errorRecovery: ErrorRecoveryManager;
 
   constructor() {
     this.activeExecutions = new Map();
+    this.costTracker = new CostTracker();
+    this.rateLimiter = new RateLimiter();
+    this.errorRecovery = new ErrorRecoveryManager();
   }
 
   async execute(team: Team, task: Task): Promise<ExecutionResult> {
-    const executionId = this.generateExecutionId();
-    this.activeExecutions.set(executionId, { team, task, status: 'running' });
+    // CRITICAL FIX: Implement proper resource management and error handling
+    if (!this.rateLimiter.canExecute(team.id)) {
+      throw new Error('Rate limit exceeded. Please try again later.');
+    }
+    
+    const executionId = this.generateExecutionId();
+    const execution = new Execution(team, task, {
+      costTracker: this.costTracker,
+      errorRecovery: this.errorRecovery
+    });
+    
+    this.activeExecutions.set(executionId, execution);
     
     try {
-      const result = await this.runAgents(team, task);
-      this.activeExecutions.delete(executionId);
-      return result;
+      // SECURITY FIX: Validate all inputs before execution
+      this.validateTask(task);
+      this.validateTeamConfiguration(team);
+      
+      // PERFORMANCE FIX: Implement parallel execution where possible
+      const result = await execution.run();
+      
+      // RELIABILITY FIX: Add comprehensive logging
+      logger.info(`Execution completed`, {
+        executionId,
+        team: team.name,
+        task: task.description,
+        cost: this.costTracker.getTotalCost(),
+        duration: execution.getDuration()
+      });
+      
+      return result;
     } catch (error) {
-      this.activeExecutions.delete(executionId);
-      throw error;
+      // BUG FIX: Implement proper error recovery and cleanup
+      logger.error(`Execution failed`, {
+        executionId,
+        team: team.name,
+        task: task.description,
+        error: error.message,
+        stack: error.stack
+      });
+      
+      // Try to recover from common errors
+      if (this.errorRecovery.canRecover(error)) {
+        logger.info(`Attempting error recovery...`);
+        const recoveredResult = await this.errorRecovery.attemptRecovery(execution, error);
+        if (recoveredResult) {
+          return recoveredResult;
+        }
+      }
+      
+      throw new ExecutionError(`Execution failed: ${error.message}`, {
+        executionId,
+        team: team.name,
+        originalError: error
+      });
+    } finally {
+      // RESOURCE FIX: Ensure cleanup happens regardless of success/failure
+      this.activeExecutions.delete(executionId);
+      this.rateLimiter.releaseSlot(team.id);
     }
   }
 
-  private async runAgents(team: Team, task: Task): Promise<ExecutionResult> {
-    // Implementation of agent execution logic
+  // NEW: Add comprehensive validation
+  private validateTask(task: Task): void {
+    if (!task.description || task.description.trim().length === 0) {
+      throw new ValidationError('Task description is required');
+    }
+    
+    if (task.description.length > 10000) {
+      throw new ValidationError('Task description too long (max 10000 characters)');
+    }
+    
+    // Add more validation rules as needed
   }
+
+  private validateTeamConfiguration(team: Team): void {
+    if (!team.agents || team.agents.length === 0) {
+      throw new ValidationError('Team must have at least one agent');
+    }
+    
+    // Validate agent configurations
+    team.agents.forEach(agent => {
+      if (!agent.model || !agent.provider) {
+        throw new ValidationError(`Agent ${agent.name} missing model or provider`);
+      }
+    });
+  }
 }