from typing import AsyncGenerator

class Agent:
    """
     the confegratero will be present here
     then will be ssection will be contain all of the opject like context management and every detales 
     we may have mutpilsetions  
    """
    def __init__(self):
        pass
    
    async def _agentic_loop(self)->AsyncGenerator[AgentEvent]:
        